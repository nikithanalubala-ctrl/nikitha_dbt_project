sources:
  - name: source
    database: "{{ env_var('SOURCE_CATALOG') }}"
    schema: source  
    tables:
      - name: fact_sales
      - name: dim_date
      - name: dim_store
      - name: fact_returns
      - name: dim_product
      - name: dim_customer
      - name: items
      

 